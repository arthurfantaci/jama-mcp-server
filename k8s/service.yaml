---
apiVersion: v1
kind: Service
metadata:
  name: jama-mcp-server
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: jama-mcp-server
  ports:
    - name: streamable-http
      protocol: TCP
      port: 8765
      targetPort: 8765
